// Global ambient declarations.

/** Build-time version stamp injected by Vite (short git SHA, or
 *  `t<epoch>` if git isn't available). Used by the UpdateChecker to
 *  detect when a new deploy has shipped. */
declare const __APP_VERSION__: string;
